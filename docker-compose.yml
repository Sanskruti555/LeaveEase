version: '3.8'

services:
  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./Backend:/app
      #- /app/node_modules

  frontend:
    build: ./Frontend
    ports:
      - "3000:80"
    depends_on:
      - backend